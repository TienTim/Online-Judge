//使用Java的考生請注意，最外層的類別(class)需命名為 main 。
//如果遇到超乎想像的狀況，請更改編譯器試看看!! 各編譯器特性不同!!
//預設測資、隨機測資、固定測資是用來幫助除錯用的。批改時，只看暗中測資是否通過!!
#include <iostream>
using namespace std;
/*
int main() {
    string s;
    cout << s;
    char c[8] = {'E', '3', 'J', 'L', 'S', '2', 'Z', '5'};
    char d[8] = {'3', 'E', 'L', 'J', '2', 'S', '5', 'Z'};
    while (cin >> s) {
        bool iP = true;
        bool iM = true;
        for (int i=0; i<s.size()/2;++i) {
            if (s[i] != s[s.size()-1-i])
                iP = false;
            for (int j=0; j<8; ++j) {
                if (s[i] == c[j] && s[s.size()-1-i] != d[j])
                    iM = false;
            }
        }
        if (iP)
            if (iM)
                cout << s << " -- is a mirrored palindrome." << endl;
            else
                cout << s << " -- is a regular palindrome." << endl;
        else
            if (iM)
                cout << s << " -- is a mirrored string." << endl;
            else
                cout << s << " -- is not a palindrome." << endl;
        cout << endl;
    }
    return 0;
}
*/
