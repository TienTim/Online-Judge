#include <iostream>
#include <string>
using namespace std;
/*
int main() {
    int n;
    cin >> n;
    for (int i=0; i<n; ++i) {
        string s;
        cin >> s;
        int f = (s[0]-'A')*26*26 + (s[1]-'A')*26 + (s[2]-'A');
        int l = stoi(s.substr(4));
        cout << (abs(f-l) <= 100 ? "nice" : "not nice") << endl;
    }
    return 0;
}
*/
